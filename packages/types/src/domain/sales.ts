import type { RestaurantId, ItemId, SalesImportBatchId, SalesImportRowId, PosRawImportId, SnapshotId } from '../branded';
export type ImportStatus = 'PENDING' | 'PROCESSING' | 'COMPLETED' | 'FAILED';
export interface SalesImportBatch { id: SalesImportBatchId; restaurantId: RestaurantId; businessDate: string; status: ImportStatus; errorMessage: string | null; fileUrl: string | null; storagePath: string | null; uploadedBy: string | null; originalFileName: string | null; totalRows: number | null; importedRows: number | null; createdAt: string; updatedAt: string; }
export interface SalesImportRow { id: SalesImportRowId; batchId: SalesImportBatchId; rawItemName: string; quantitySold: number; isMapped: boolean; createdAt: string; }
export interface PosRawImport { id: PosRawImportId; batchId: SalesImportBatchId; plu: number | null; charakteristika1: string | null; charakteristika2: string | null; barcode: string | null; nazov: string; pluTypeNumber: number | null; pluTypeText: string | null; groupNumber: number | null; groupName: string | null; outletNumber: number | null; outletName: string | null; quantity: number; uom: string | null; totalPriceExclVat: number | null; totalPriceInclVat: number | null; totalCogs: number | null; originalPriceInclVat: number | null; totalDiscount: number | null; optionalText1: string | null; optionalText2: string | null; optionalText3: string | null; rawJson: Record<string, unknown> | null; createdAt: string; }
export interface DailyInventorySnapshot { id: SnapshotId; restaurantId: RestaurantId; itemId: ItemId; businessDate: string; eodQty: number; fifoTotalValue: number; createdAt: string; }
export interface VarianceAnalyticRow { restaurantId: RestaurantId | null; itemId: ItemId | null; reportingMonth: string | null; actualQty: number | null; theoreticalQty: number | null; unexplainedVarianceQty: number | null; }
