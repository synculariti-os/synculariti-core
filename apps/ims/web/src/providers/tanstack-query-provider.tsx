'use client';

import React from 'react';

// Tanstack Query provider stub — install @tanstack/react-query and wire up when ready
export function TanstackQueryProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
